# Full sweep configuration for MAML-TRPO
program: maml_trpo_metaworld.py
method: bayes
metric:
  name: adaptation_reward
  goal: maximize
parameters:
  adapt_lr:
    distribution: log_uniform_values
    min: 0.001
    max: 0.9
  meta_lr:
    distribution: log_uniform_values
    min: 0.1
    max: 0.9
  adapt_steps:
    values: [1, 3, 5]

  meta_bsz:
    distribution: q_log_uniform_values
    q: 10
    min: 10
    max: 30
  adapt_bsz:
    distribution: q_log_uniform_values
    q: 10
    min: 10
    max: 30

  tau:
    distribution: uniform
    min: 0.90
    max: 1.0
  gamma:
    distribution: uniform
    min: 0.90
    max: 0.99

  num_iterations:
    value: 30 # 10% from a full run = 600 meta iterations max 24 hours
  
  seed:
    value: 42
  num_workers:
    value: 10
  cuda:
    value: True